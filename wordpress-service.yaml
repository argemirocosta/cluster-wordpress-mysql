apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: default
  labels:
    app: wordpress-deployment
spec:
  type: NodePort
  selector:
    app: wordpress-deployment
  ports:
  - port: 80
    targetPort: 80
    name: wordpress
